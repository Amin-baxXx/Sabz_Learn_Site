@import "variables.css";
@import "module/topBar.css";
@import "module/nav-bar.css";
@import "fonts.css";
@import "module/default.css";
@import "module/footer.css";
@import "module/course-box.css";
/*main */

/* Landing */
.landing__title {
  color: var(--white-color);
  margin-top: 5rem;
  padding-top: 6rem;
  text-align: center;
  font-size: 4rem;
}
.landing__subtitle {
  text-align: center;
  padding-top: 1rem;
  color: var(--white-color);
  font-size: 1.8rem;
}
.landing {
  background:
    linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
    url("../images/backGround/background_landing.jfif"),
    no-repeat center;
  background-size: cover;
  height: 51rem;
}
.landing__searchbar {
  text-align: center;
  margin: 3rem auto;
  background: var(--white-color);
  width: 80rem;
  padding: 0.5rem 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-radius: 1rem;
}
.landing__search-input {
  border: none;
  outline: none;
  width: 100%;
  padding-right: 1rem;
}
.landing__searchbar-btn {
  display: flex;
  justify-content: center;
  align-items: center;
  width: 6rem;
  height: 5rem;
  border-radius: 0.5rem;
  background: var(--primary-color);
  border: none;
  outline: none;
  color: var(--white-color);
  font-size: 2.2rem;
}
.landing-status {
  color: var(--white-color);
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 4rem;
  /*background: red;*/
}
.landing-status__item {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 171px;
}
.landing-status__count {
  font-size: 3rem;
  margin-top: 1rem;
}
/* ُStart latest courses Styles*/
.courses {
}
.courses-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 4rem;
}
.courses-header__right {
  display: flex;
  flex-direction: column;
}
.courses-header__text {
  color: #9c9c9c;
  font-size: 1.5rem;
}
.courses-header__link {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--white-color);
  background-color: var(--primary-color);
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  gap: 1rem;
  border: 2px solid var(--primary-color);
  transition:
    color linear 300ms,
    background-color linear 300ms;
}
.courses-header__link:hover {
  color: var(--primary-color);
  background-color: var(--white-color);
}
/* Finish latest courses Styles*/

/*aboutUS */
.about-us {
}
.about-us__header {
  display: flex;
  flex-direction: column;
}
.about-us__title {
  width: max-content;
}
.about-us__subtitle {
  color: #9c9c9c;
  font-size: 1.5rem;
  margin: 1rem 0 3rem 0;
}
.about-us__box {
  display: flex;
  align-items: center;
  box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.1);
  border-radius: 0.5rem;
  gap: 1rem;
  padding: 2rem;
}
.about-us-box__right {
}
.about-us-box__icon {
  font-size: 6rem;
  color: #666;
}
.about-us-box__left {
  display: flex;
  flex-direction: column;
}
.about-us-box__title {
  font-weight: bold;
}
.about-us-box__text {
  font-size: 1.4rem;
  font-weight: 500;
}
/*slider*/
.popular__slider .courses-box,
.presell__slider .courses-box {
  margin: 4rem 1.3rem 0 1.3rem;
  box-shadow: 0 0 10px rgba(168, 172, 185, 0.3);
}
.popular__slider .courses-box:hover {
  transform: none;
}
.popular__slider .courses-box {
  cursor: grab;
}
.popular__slider .courses-box:active {
  cursor: grabbing;
}

/*.popular__slider:active {*/
/*  cursor: grabbing;*/
/*}*/
.course-box__img {
  width: 100%;
  height: 205px !important;
  object-fit: cover;
}
/*Start articles header Style*/
.articles {
  margin: 4rem 0;
}

.articles-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.articles-header__right {
  display: flex;
  flex-direction: column;
}

.articles-header__text {
  color: #9c9c9c;
  font-size: 1.5rem;
}

.articles-header__link {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--white-color);
  background-color: var(--primary-color);
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  border: 2px solid var(--primary-color);
  transition:
    color linear 300ms,
    background-color linear 300ms;
}

.articles-header__link:hover {
  color: var(--primary-color);
  background-color: var(--white-color);
}
.articles-header__icon {
  margin-right: 0.5rem;
  font-size: 1.5rem;
}

.article-card {
  margin: 3rem 0;
  height: 430px;
  transition: all 400ms ease;
  border-radius: 1rem;
  box-shadow: 0 5px 30px rgba(70, 72, 77, 0.08);
}

.article-card:hover {
  transform: translateY(-0.6rem);
}

.article-card__img {
  object-fit: cover;
  width: 100%;
  border-radius: 1rem 1rem 0 0;
  height: 238px;
}

.article-card__content {
  padding: 1rem 2rem 3rem 2rem;
}

.article-card__link {
  font-weight: bold;
}

.article-card__text {
  color: #898989;
  font-size: 1.2rem;
  padding: 1rem 0 2.5rem 0;
}

.article-card__btn {
  color: var(--primary-color);
  border: 2px solid var(--primary-color);
  font-size: 1.4rem;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  transition: all 400ms ease;
}

.article-card__btn:hover {
  color: var(--white-color);
  background-color: var(--primary-color);
}

/* Finish Articles Styles */
/*ّFooter*/

/* Start Helper Classes*/
.title {
  margin: 4rem 0 0 0;
  font-size: 2.3rem;
  color: #444446;
  font-weight: bold;
  line-height: 1.2;
  position: relative;
  display: inline-block;
}

.title:before {
  content: "";
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 55%;
  opacity: 0.3;
  z-index: -1;
  background-color: var(--primary-color);
}
.title:after {
  content: "";
  position: absolute;
  right: -1rem;
  top: -4px;
  transform: rotate(8deg);
  width: 4px;
  height: 45px;
  border-radius: 1rem;
  z-index: -1;
  background-color: var(--primary-color);
}

.diamond:after {
  content: "";
  position: absolute;
  width: 3.5rem;
  height: 3.5rem;
  background-color: var(--primary-color);
  border-radius: 0.7rem;
  opacity: 0.3;
  transform: rotate(45deg);
  right: 1rem;
}

/* Finish Helper Classes*/
