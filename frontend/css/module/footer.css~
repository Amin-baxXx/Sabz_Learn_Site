.footer {
    background-color: #f0f2f7;
    border-radius: 1rem;
    padding: 4rem 3.5rem;
    margin-bottom: 5rem;
    position: relative;
}
.about-us__footer {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}
.about-us__footer-text {
    color: #7d7e7f;
}
.about-us__footer-title {
    position: relative;
    font-size: 1.8rem;
    font-weight: bold;
}
.footer__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    column-gap: 2rem;
}
.latest-news__footer {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}
.latest-news__footer-title {
    position: relative;
    font-size: 1.7rem;
    font-weight: bold;
}
.latest-news__footer-item-flex {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}
.quick-access__footer__double-column {
    display: flex;
    flex-direction: column;
    gap: 3rem;
}
.quick-access__flex {
    display: flex;
    gap: 4rem;
}
.quick-access__footer-title {
    position: relative;
    display: inline-block;
    margin-bottom: 2rem;
    font-size: 1.7rem;
    font-weight: bold;
}
.quick-access__right {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}
.quick-access__left {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}
.subscription-footer__box {
    width: 100%;
    border: 1px solid #b8b8b8;
    border-radius: 0.5rem;

    overflow: hidden;
    display: flex;
    justify-content: space-between;
}
.subscription-footer__title {
    position: relative;
    font-size: 1.7rem;
    font-weight: bold;
}
.subscription-footer__text {
    margin: 1.5rem 0;
    color: #7d7e7f;
    font-size: 1.6rem;
}
.subscription-footer__input {
    border: none;
    background: transparent;
}
.subscription-footer__button {
    padding: 1rem 0;
    width: 20%;
    border: none;
    background: #7d7e7f;
    color: #fff;
    transition: 200ms linear background-color;
}
.subscription-footer__button:hover {
    background-color: var(--primary-color);
}
.footer:before {
    content: "";
    position: absolute;
    right: 0;
    left: 0;
    margin: 0 auto;
    height: 4rem;
    width: 50rem;
    background-color: var(--primary-color);
    z-index: -1;
    top: 50.2rem;
    border-radius: 5rem;
}
.footer__copyright {
    background-color: #f0f2f7;
    text-align: center;
    padding: 3rem;
}
.footer__copyright-text {
}
/*Start buty style footer*/
